mixin card(c)
    pre.square(
        class=`${c.status}`
        data-status=`${c.status}`
        data-code=`${c.type}`
        data-fights="0"
        data-attack=`${cards.types[c.type].attack}`
        data-defense=`${cards.types[c.type].defense}`
        )&attributes(attributes)
        if c.status == 'ready'
            .
                #{c.type}
                A: #{cards.types[c.type].attack}
                D: #{cards.types[c.type].defense}
        if c.status == 'face-down'
            .
                ???
